# Arithma

A math-focused programming language.

## Current syntax

```
x = 5

f(x) = x^2 + 2x + 1

output(x)
output(f(3))
output(f)
```

## Run

```bash
pip install -r requirements.txt
python main.py example.ari
```

Arithma source files use the `.ari` extension.